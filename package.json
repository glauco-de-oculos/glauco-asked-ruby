{
  "name": "glauco-workspace",
  "private": true,
  "scripts": {
    "build": "bundle exec glauco-package build --entry bin/main.rb --app-name glauco-app --output dist --image ghcr.io/CHANGE_ME/glauco/glauco-app:latest --namespace glauco",
    "web:install": "npm --prefix core/framework/web install",
    "web:build": "npm --prefix core/framework/web run build:carbon"
  }
}
