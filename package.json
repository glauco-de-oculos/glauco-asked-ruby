{
  "name": "glauco-workspace",
  "private": true,
  "scripts": {
    "web:install": "npm --prefix core/framework/web install",
    "web:build": "npm --prefix core/framework/web run build:carbon"
  }
}
